@model IdentityBase.Actions.Register.SuccessViewModel

<div class="login-page">
    <div class="page-header">
        <h1 localize="">Register</h1>
    </div>

    <p localize="">Please check your mail box for confirmation email.</p>

    @if (Model.Provider != null)
    {
        <a href="@Model.Provider.WebMailBaseUrl">@Model.Provider.Provider</a>
    }
</div>  