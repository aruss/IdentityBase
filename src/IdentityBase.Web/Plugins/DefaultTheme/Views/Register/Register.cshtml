@model IdentityBase.Actions.Register.RegisterViewModel
<div class="login-page">
    <div class="page-header">
        <h1 localize="">Register</h1>
    </div>
    @Html.Partial("_ValidationSummary")
    <div class="row">
        <div class="col-sm-6">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title" localize="">
                        Local Account
                    </h3>
                </div>
                <div class="panel-body">
                    <form asp-controller="Register"
                          asp-action="Register"
                          asp-route-returnUrl="@Model.ReturnUrl"
                          novalidate>
                        <fieldset>
                            @foreach (var component in Model.FormModel.FormElements)
                            {
                                await Html.RenderPartialAsync(component.ViewName, component.Model);
                            }
                        </fieldset>
                    </form>
                    <ul class="list-inline">
                        <li show-if="@Model.EnableLocalLogin">
                            <a asp-route="Login"
                               asp-route-returnUrl="@Model.ReturnUrl"
                               localize="">Login</a>
                        </li>
                        <li show-if="@Model.EnableAccountRecover">
                            <a asp-route="Recover"
                               asp-route-returnUrl="@Model.ReturnUrl"
                               localize="">Forgot password?</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
        @Html.Partial("_ExternalLogins", Model)
    </div>
</div>